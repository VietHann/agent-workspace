# Project context

Generated by agent-workspace. Re-run `agent-workspace init --force` to refresh detected data.

## Detected facts

- **Supabase** (database) — detected; evidence: `package.json:@supabase/supabase-js`
- **Next.js** (framework) — detected; evidence: `package.json:next`
- **React** (framework) — detected; evidence: `package.json:react`
- **TypeScript** (language) — detected; evidence: `package.json or tsconfig.json`
- **pnpm** (package-manager) — detected; evidence: `pnpm-lock.yaml`
- **Vitest** (testing) — detected; evidence: `package.json:vitest`

## Repository layout

- Source directories: `src`
- Test directories: `tests`
- Package boundaries: None detected

## Commands

- `build`: `next build` — evidence: `package.json`
- `test`: `vitest run` — evidence: `package.json`
- `typecheck`: `tsc --noEmit` — evidence: `package.json`

## Conventions

- TypeScript strict mode is enabled. — detected; evidence: `tsconfig.json`

## Editable recommendations

- Treat dependency and file signals as repository evidence, not proof of runtime architecture.
